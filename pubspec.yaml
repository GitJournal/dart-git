name: dart_git
description: A pure dart implementation of Git
version: 0.0.2
homepage: https://gitjournal.io

dependencies:
  path: ^1.8.0
  crypto: ^3.0.0
  args: ^2.0.0
  buffer: ^1.1.0
  equatable: ^2.0.0
  file: ^6.1.0
  tuple: ^2.0.0
  quiver: ^3.0.0
  collection: ^1.15.0
  freezed_annotation: ^0.14.1
  meta: ^1.3.0

dev_dependencies:
  pedantic: ^1.11.0
  test: ^1.16.8
  import_sorter: ^4.5.1
  process_run: ^0.12.1
  archive: ^3.1.2
  freezed: ^0.14.1+2
  build_runner:

environment:
  sdk: ">=2.12.0 <3.0.0"

import_sorter:
  comments: false
